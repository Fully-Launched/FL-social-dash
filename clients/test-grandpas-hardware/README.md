# TEST CLIENT — not real

"Grandpa's Hardware" is a made-up **concierge-system** client — the "someone
like my dad" case from `SYSTEM-BLUEPRINT.md`'s System 2. Used to prove the
concierge path through the dashboards: no concept-approval gate, no
self-filming workflow, straight to Tait filming an interview, then the same
editor → owner-review → client-final-review → posting pipeline as the
self-serve system. No real business, no real interview happened here —
everything in `dashboards/clients/test-grandpas-hardware/config.json` is
fabricated for testing.

Delete this folder and `dashboards/clients/test-grandpas-hardware/` once the
concierge path is confirmed working, same as `test-loop-coffee`.
